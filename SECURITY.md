# 安全与隐私

## 报告安全问题

请不要在公开 Issue 中粘贴真实账号、邮箱口令、Access Token、Refresh Token、
浏览器 Cookie、代理凭据、支付资料、`state.json` 或 `state_data`。安全问题请通过
GitHub Security Advisory 的私密报告入口提交，并仅提供能够复现问题的最小脱敏样例。

## 本地敏感数据

运行数据默认保存在 `%APPDATA%\OpenAIRegister`，不属于源码。提交代码前请确认
Git 暂存区中没有状态文件、导出文件、日志、截图、浏览器配置目录或真实服务凭据。
仓库中的测试 Token、邮箱和代理地址必须是不可用于真实服务的合成夹具。

## 使用边界

本项目仅用于用户有权管理的账号和服务。涉及注册、代理、租号、Team 席位、
checkout 或支付的操作可能产生费用或不可逆远端状态，使用者应遵守相关服务条款，
并在执行前核对应用内确认信息。
